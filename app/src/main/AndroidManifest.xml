<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vku_demo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vku_demo">

        <activity
            android:name=".ui.gretting.MainActivity"
            android:exported="true">

        </activity>

        <activity android:name=".ui.newspaper.NewsActivity"
            android:exported="true">

        </activity>

        <activity android:name=".ui.exam.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.exam.InformationActivity">

        </activity>
    </application>
</manifest>
